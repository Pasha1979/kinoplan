# 🔬 АНАЛИЗ КОНКУРЕНТОВ — Редактор сценария

> Глубокий анализ механик написания, распознавания и автозамен у трёх ключевых конкурентов

---

## 📋 СВОДНАЯ ТАБЛИЦА ПО КОНКУРЕНТАМ

| Функция | КИТ Сценарист | Filmtoolz | Final Draft |
|---------|---------------|-----------|-------------|
| **Распознавание шапки** | Достаточно набрать «НАТ.» или «ИНТ.» | По отступам и капслоку | «IN» или «EX» → автоформат в Scene Heading |
| **Авто INT/EXT** | «и» или «н» в начале строки → подсказки | — | «E» или «I» → подсказки EXT/INT |
| **База локаций** | ✅ Сохраняются в проекте, автоподстановка | Распознаются при импорте | ✅ SmartType список всех локаций |
| **Время суток** | При «—» или Tab или первой букве → УТРО/ДЕНЬ/ВЕЧЕР/НОЧЬ | — | Tab после локации → подсказки времени |
| **Переключение типов** | Enter/Tab между блоками | Кнопка «F» (Format) | Tab между элементами шапки |
| **SmartType персонажей** | Автоподстановка сохранённых | При импорте | ✅ SmartType список всех имён |
| **База персонажей** | ✅ Отдельный справочник | — | ✅ Автосоздание списка |
| **Отключение авто** | ✅ Настройки → отключить автозамены | — | ✅ Настраивается |
| **Переходы (transition)** | — | Распознаются по капслоку | ✅ SmartType список |
| **Ремарки** | — | В скобках без пустых строк | Tab на персонаже → (V.O.), (O.S.) |

---

## 🎯 КЛЮЧЕВЫЕ МЕХАНИКИ ПО КОНКУРЕНТАМ

### **КИТ Сценарист** (Российский стандарт)

```
📝 ШАПКА СЦЕНЫ:
Начало строки + «и» → подсказка «ИНТ.»
Начало строки + «н» → подсказка «НАТ.»
После ИНТ./НАТ. → подсказка ранее введённых локаций
При нажатии «—» (тире) или Tab → подсказка времени (УТРО/ДЕНЬ/ВЕЧЕР/НОЧЬ)
Первые буквы времени → фильтрация подсказок

✨ ОСОБЕННОСТИ:
- Сохранение пользовательского времени/места для проекта
- Автоподстановка через Tab/Enter
- Esc для отказа
- Можно отключить в настройках
```

### **Filmtoolz** (Импорт-ориентированный)

```
📝 РАСПОЗНАВАНИЕ ПО ОТСТУПАМ:

Пустая строка + текст = ДЕЙСТВИЕ (action)
Пустая строка + КАПСЛОК + следующая строка = ПЕРСОНАЖ
Без пустой строки после Персонажа = ДИАЛОГ
Скобки без пустой строки = РЕМАРКА

⚠️ ОШИБКИ РАСПОЗНАВАНИЯ:
- Любой текст ЗАГЛАВНЫМИ + следующая строка = Персонаж
- Любой текст между пустыми строками = Действие
- После Персонажа пустая строка = Действие (ошибка)
- Перед Ремаркой/Диалогом пустая строка = Действие (ошибка)

🛠️ ИНСТРУМЕНТЫ:
Кнопка «W» — режим сохранения форматирования Word
Кнопка «F» — автоформатирование (разовое)
Ручное назначение стиля через панель
```

### **Final Draft** (Голливудский стандарт)

```
📝 SMARTTYPE — ГЛАВНАЯ ФИШКА:

При наборе в Scene Heading:
«I» или «E» → подсказка INT./EXT.
Tab после INT/EXT → переход к Location
При наборе Location → подсказка из базы
Tab после Location → подсказка времени

📚 АВТОСОЗДАНИЕ БАЗ:
- SmartType создаёт списки автоматически
- Персонажи, локации, время, переходы
- Подсказки при наборе любых первых букв
- Command/Ctrl + 3 → список персонажей

🎭 ПЕРЕХОДЫ МЕЖДУ ЭЛЕМЕНТАМИ:
Tab на Character → Parenthetical (ремарка)
Tab после Character cue → Dialogue
Insert Character → автоматически создаёт Dialogue

🎨 НАСТРОЙКА SMARTTYPE:
- Автоматическое угадывание следующего персонажа
- Tab переход к Character Extension (V.O., O.S.)
- Настройка порядка списков
- Цвета подсказок
- Сортировка по количеству диалогов
```

---

## 🧩 ГИБРИДНАЯ МОДЕЛЬ ДЛЯ КИНОПЛАН

### **Фаза 1: Шапка сцены (сейчас)**

```
✅ РЕАЛИЗОВАТЬ:

1. РАСПОЗНАВАНИЕ ПО МАРКЕРАМ (как КИТ + Final Draft)
   - Начало строки + «И» → подсказка «ИНТ.»
   - Начало строки + «Э» → подсказка «ЭКСТ.»
   - Начало строки + «IN» → подсказка «INT.»
   - Начало строки + «EX» → подсказка «EXT.»
   - Если уже scene_header — не меняем тип при редактировании!

2. ПОДСКАЗКИ БЕЗ ВМЕШАТЕЛЬСТВА (как Final Draft SmartType)
   - Подсказка появляется, но не вставляется автоматически
   - Tab/Enter — принять подсказку
   - Esc — отказаться и писать дальше
   - Пробел/другие клавиши — просто продолжить набор

3. АВТО-СОХРАНЕНИЕ ЛОКАЦИЙ (как КИТ + Final Draft)
   - Каждая новая локация сохраняется в проект
   - При наборе после ИНТ./ЭКСТ. → подсказка из базы

4. АВТО-ПОДСТАНОВКА ВРЕМЕНИ (как КИТ)
   - При наборе «—» (тире) или Tab после локации
   - Подсказки: УТРО, ДЕНЬ, ВЕЧЕР, НОЧЬ
   - Фильтрация по первым буквам

5. РУЧНОЕ ПЕРЕКЛЮЧЕНИЕ (как Filmtoolz)
   - Кнопка переключения типа блока
   - Не меняем автоматически при редактировании!
```

### **Фаза 2: Полный редактор (будущее)**

```
⏳ ЗАПЛАНИРОВАНО:

- Справочник персонажей (как КИТ)
- Справочник локаций с описаниями
- Справочник времени суток + пользовательские
- Участники сцены через запятую
- Кнопка автоформатирования «F» (как Filmtoolz)
- SmartType для всех элементов (как Final Draft)
- Переходы Tab между элементами
- Ремарки на Tab от персонажа
- Настройки автоформатирования
```

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ НАШЕГО РЕДАКТОРА

### **Правило 1: НЕ МЕШАТЬ АВТОРУ**
```
❌ ЗАПРЕЩЕНО:
- Автоматическая замена текста без подтверждения
- Изменение типа блока при редактировании текста
- Блокировка пробелов или других символов
- Принудительное форматирование

✅ РАЗРЕШЕНО:
- Показывать подсказку — автор сам решает
- Tab/Enter — принять подсказку
- Esc — отказаться
- Пробел/буквы — продолжить писать
```

### **Правило 2: ТИП БЛОКА = ВЫБОР АВТОРА**
```
- Тип определяется ПРИ СОЗДАНИИ блока
- При редактировании тип НЕ меняется автоматически
- Тип меняется ТОЛЬКО явно (Tab или кнопка)
- Шапка сцены остаётся шапкой пока автор не скажет Tab
```

### **Правило 3: SMARTTYPE = ПОМОЩНИК**
```
- Подсказки появляются при совпадении первых букв
- Подсказки из базы проекта + стандартные
- Не обязательно принимать — можно писать своё
- База пополняется автоматически
```

---

## 🎬 ПРИМЕР РАБОТЫ (целевой)

```
1. Пустой блок типа scene_header
   ↓
2. Автор пишет: «Э»
   ↓
3. ПОЯВЛЯЕТСЯ подсказка: [ЭКСТ.]
   ↓
4. Автор нажимает Tab → текст: «ЭКСТ. » (с пробелом)
   ИЛИ автор пишет дальше «КСТ. УЛИЦА» (игнорирует подсказку)
   ↓
5. После «ЭКСТ. » автор пишет «У»
   ↓
6. ПОЯВЛЯЕТСЯ подсказка: [УЛИЦА] [УНИВЕРСИТЕТ] (из базы)
   ↓
7. Автор выбирает или пишет своё
   ↓
8. После локации нажимает Tab или «—»
   ↓
9. ПОЯВЛЯЕТСЯ подсказка: [ДЕНЬ] [НОЧЬ] [УТРО] [ВЕЧЕР]
   ↓
10. Автор выбирает или пишет «ДЕНЬ 1»
   ↓
11. Нажимает Tab → новый блок action (или character)
```

---

## 📌 ИТОГ АНАЛИЗА

| Конкурент | Лучшая фича | Берём |
|-----------|-------------|-------|
| **КИТ Сценарист** | Авто-подсказка времени при «—» или Tab | ✅ Да |
| **КИТ Сценарист** | Сохранение локаций/времени в проекте | ✅ Да |
| **Filmtoolz** | Кнопка «F» автоформатирования | ✅ Да (Фаза 2) |
| **Filmtoolz** | Распознавание по отступам | ⚠️ Изучить |
| **Final Draft** | SmartType с принятием/отказом | ✅ Да |
| **Final Draft** | Tab-переходы между элементами | ✅ Да |
| **Final Draft** | Автосоздание баз из текста | ✅ Да (Фаза 2) |

---

## 🎬 РАБОТА СО СЦЕНАМИ КАК СТРУКТУРНЫМИ ЕДИНИЦАМИ

### **КИТ Сценарист**
```
📁 СТРУКТУРА СЦЕН:
- Навигатор по сериям (для сериалов)
- Список сцен с номерами и локациями
- Переход к сцене по клику
- Автонумерация сцен при добавлении

⚠️ ОГРАНИЧЕНИЯ:
- Сцены = только шапки сцен
- Нет Drag&Drop сцен
- Нет группировки сцен
- Нет визуализации структуры
```

### **Filmtoolz**
```
📁 СТРУКТУРА СЦЕН:
- Scene Navigator — список сцен с фильтрами
- Фильтрация по типу (INT/EXT)
- Фильтрация по локации
- Фильтрация по персонажам
- Drag&Drop сцен для изменения порядка

✨ ОСОБЕННОСТИ:
- Сцены распознаются по шапкам
- Можно менять порядок сцен
- Автообновление номеров при перестановке
- Week at a Glance — таймлайн сцен
```

### **Final Draft**
```
📁 СТРУКТУРА СЦЕН:
- Scene Navigator — слева от редактора
- Список сцен с номерами, локациями, хронометражем
- Переход к сцене по клику
- Drag&Drop сцен для изменения порядка
- Цветная маркировка сцен (ревизии)

✨ ОСОБЕННОСТИ:
- Сцены = структурные узлы документа
- Автообновление номеров при перестановке
- Синхронизация с редактором в реальном времени
- Scene Statistics — хронометраж по сценам
- Scene Properties — редактирование метаданных сцены
```

### **SetHero**
```
📁 СТРУКТУРА СЦЕН:
- Week at a Glance — горизонтальный таймлайн
- Сцены как карточки на таймлайне
- Drag&Drop сцен на таймлайне
- Автообновление хронометража

✨ ОСОБЕННОСТИ:
- Визуализация структуры сцен
- Сцены = карточки с метаданными
- Интерактивный таймлайн
```

---

## 🧩 СРАВНЕНИЕ ВАРИАНТОВ Б И В

### **Вариант Б: Парсер сцен (text-based)**
```
📋 ЛОГИКА:
- Парсер сканирует текст
- Находит шапки сцен по паттерну
- Собирает содержимое между шапками
- Создает объекты сцен

✅ ПЛЮСЫ:
- Простой в реализации
- Работает с любым текстом
- Легко импортировать/экспортировать
- Совместим с Final Draft (.fdx)

❌ МИНУСЫ:
- Тяжелая операция (debounced)
- Не работает в реальном времени
- Потенциальные ошибки парсинга
- Нет синхронизации с редактором
- Drag&Drop требует сложной логики
```

### **Вариант В: Tiptap Document API (node-based)**
```
📋 ЛОГИКА:
- Сцены = отдельные узлы в дереве документа
- Tiptap SceneNode = контейнер для сцены
- Дочерние узлы = блоки сцены
- Document API для навигации

✅ ПЛЮСЫ:
- Работает в реальном времени
- Синхронизация с редактором
- Drag&Drop из коробки (Tiptap Draggable)
- Структура документа = структура сцен
- Легко расширять (метаданные, комментарии)
- Undo/redo для сцен

❌ МИНУСЫ:
- Сложная интеграция
- Требует переписывания логики
- Экспорт/импорт требует конвертации
- Меньше совместимость с .fdx
```

---

## 🎯 РЕКОМЕНДАЦИЯ: ГИБРИДНЫЙ ПОДХОД

### **Фаза 1: Парсер сцен (Вариант Б) — сейчас**
```
✅ ДЛЯ MVP:
- Парсер собирает сцены из текста
- Навигатор показывает список сцен
- Переход к сцене по клику
- Debounced 300ms — приемлемо

⏳ В БУДУЩЕМ:
- Drag&Drop сцен через парсер
- Автообновление номеров
```

### **Фаза 2: Tiptap SceneNode (Вариант В) — масштабирование**
```
✅ ДЛЯ ПРОФЕССИОНАЛЬНОЙ ВЕРСИИ:
- Сцены как узлы документа
- Drag&Drop из коробки
- Комментарии к сценам
- Метаданные сцен
- Совместное редактирование

🔄 МИГРАЦИЯ:
- Конвертация парсера → SceneNode
- Сохранение совместимости с .fdx
```

---

## 🚀 ЛУЧШЕЕ РЕШЕНИЕ ДЛЯ СЦЕНАРИСТА

### **Критерии удобства:**
1. **Скорость** — мгновенный отклик
2. **Надежность** — не терять сцены
3. **Гибкость** — менять порядок сцен
4. **Визуализация** — видеть структуру
5. **Совместимость** — импорт/экспорт

### **Рекомендация:**
```
🎯 НАЧАТЬ С ВАРИАНТА Б (парсер):
- Быстрый старт (1-2 дня)
- Работает для MVP
- Навигатор + переходы

🎯 ПЕРЕЙТИ НА ВАРИАНТ В (SceneNode):
- Когда нужна функциональность Drag&Drop
- Когда нужны комментарии к сценам
- Когда нужна синхронизация в реальном времени

🎯 СОХРАНИТЬ ПАРСЕР ДЛЯ ИМПОРТА:
- .fdx → текст → SceneNode
- Обратная конвертация
- Совместимость с Final Draft
```

---

## 🎯 КРИТИЧЕСКИЕ ТРЕБОВАНИЯ КИНОПЛАН

### **Требование 1: Авто-нумерация серий**
```
📋 ЛОГИКА:
- При создании проекта указывается кол-во серий
- В редакторе пользователь выбирает серию (1, 2, 3...)
- Автор пишет: "1. ИНТ. КВАРТИРА — ДЕНЬ"
- Программа автоматически исправляет на: "1-1. ИНТ. КВАРТИРА — ДЕНЬ"
- Номер серии добавляется автоматически

✅ ЦЕЛЬ:
- Упростить написание для сериалов
- Избежать ошибок в нумерации
- Автоматическое форматирование
```

### **Требование 2: Drag&Drop сцен с авто-перенумерацией**
```
📋 ЛОГИКА:
- Сцены в навигаторе: 1-1, 1-2, 1-3, 1-4, 1-5
- Пользователь перетаскивает сцену 1-5 на место 1-2
- Авто-перенумерация:
  - Бывшая 1-5 → 1-2 (теперь вторая)
  - Бывшая 1-2 → 1-3 (теперь третья)
  - Бывшая 1-3 → 1-4 (теперь четвертая)
  - Бывшая 1-4 → 1-5 (теперь пятая)
- Сцены меняются местами и в навигаторе, и в редакторе
- Текст перемещается вместе с номерами

✅ ЦЕЛЬ:
- Гибкая работа со структурой
- Авто-обновление номеров
- Синхронизация навигатора и редактора
```

---

## 🚨 ВЛИЯНИЕ НА ВЫБОР АРХИТЕКТУРЫ

### **Вариант Б (Парсер сцен) — НЕПОДХОДИТ!**

**Требование 1 (Авто-нумерация серий):**
```
✅ МОЖНО РЕАЛИЗОВАТЬ:
- Паттерн замены при печати
- "1. ИНТ." → "1-1. ИНТ." (если выбрана серия 1)
- Debounced 300ms — приемлемо
```

**Требование 2 (Drag&Drop с авто-перенумерацией):**
```
❌ СЛОЖНО/НЕВОЗМОЖНО:
- Переставить текст в редакторе
- Репарсить весь документ
- Обновить номера сцен
- Синхронизировать с навигатором
- Debounced 300ms — задержка при каждом Drag&Drop
- Потенциальные ошибки при репарсинге
- Сложная логика перестановки текста

⚠️ ПРОБЛЕМЫ:
- Пользователь перетаскивает → задержка 300ms
- Репарсинг может ошибиться
- Текст может потеряться при перестановке
- Нет синхронизации в реальном времени
```

### **Вариант В (Tiptap SceneNode) — ИДЕАЛЬНО!**

**Требование 1 (Авто-нумерация серий):**
```
✅ ЛЕГКО РЕАЛИЗОВАТЬ:
- Атрибут `seriesNumber` в SceneNode
- При печати "1. ИНТ." → автоматически добавляет seriesNumber
- Мгновенное обновление
- Можно отключить для фильмов
```

**Требование 2 (Drag&Drop с авто-перенумерацией):**
```
✅ ИЗ КОРОБКИ (Tiptap Draggable):
- Сцены как узлы документа
- Drag&Drop переставляет узлы
- Номера обновляются через атрибуты
- Синхронизация мгновенная
- Текст перемещается вместе с узлами
- Никаких ошибок репарсинга

🎯 РЕАЛИЗАЦИЯ:
const SceneNode = Node.create({
  draggable: true, // Drag&Drop из коробки
  
  addAttributes() {
    return {
      seriesNumber: { default: 1 },
      sceneNumber: { default: 1 },
    }
  },
})

// При перестановке — авто-перенумерация
editor.on('transaction', ({ transaction }) => {
  // Если были изменения порядка сцен
  // Пересчитываем номера
  renumberScenes()
})
```

---

## 🎯 ИЗМЕНЕННАЯ РЕКОМЕНДАЦИЯ

### **Сразу Вариант В (Tiptap SceneNode)!**

**Почему:**
- Требование 2 (Drag&Drop с авто-перенумерацией) критически важно
- Парсер сцен НЕ сможет это реализовать качественно
- SceneNode решает оба требования идеально
- Tiptap Draggable — проверенная технология

**План:**
```
ФАЗА 1: SceneNode базовый (1 неделя)
- Создать SceneNode
- Авто-нумерация серий
- Навигатор + переходы

ФАЗА 2: Drag&Drop сцен (3-5 дней)
- Tiptap Draggable
- Авто-перенумерация
- Синхронизация навигатора

ФАЗА 3: Парсер для импорта (2-3 дня)
- Импорт .fdx → SceneNode
- Экспорт SceneNode → .fdx
- Совместимость с Final Draft
```

---

*Документ создан: 29.05.2026*
*Обновлено: 01.06.2026 — добавлен анализ работы со сценами*
*Обновлено: 01.06.2026 — добавлены критические требования КиноПлан (авто-нумерация серий, Drag&Drop с авто-перенумерацией)*
*Источники: КИТ Сценарист (help_old.html), Filmtoolz (lessons/plan), Final Draft (blog), SetHero (features)*
