# 📚 LESSONS_LEARNED — Ошибки и уроки КиноПлан

> **Назначение:** Журнал всех ошибок и уроков для предотвращения повторений.
> 
> **Связанные документы:** [🏆_ЖУРНАЛ_УСПЕХОВ.md](./🏆_ЖУРНАЛ_УСПЕХОВ.md) | [📊_ТЕКУЩИЙ_СТАТУС.md](./📊_ТЕКУЩИЙ_СТАТУС.md)

---

🔗 НЕЙРОННЫЕ СВЯЗИ:
- Зависит от (читать перед изменением): `📊_ТЕКУЩИЙ_СТАТУС.md`
- Обновляет при изменении: `🏆_ЖУРНАЛ_УСПЕХОВ.md`, `ПРАВИЛА_АРХИТЕКТУРЫ.md`

---

## Сессия 30.05.2026 — SmartType для шапки сцены

### Урок 1: Автонумерация мешала вводу
**Проблема:** Переписывала номера сцен при каждом изменении, мешая пользователю.
**Решение:** Убрать из `handleContentChange`, оставить только при создании новой сцены.
**Предотвращение:** Не модифицировать пользовательский ввод без явного запроса.

### Урок 2: Пробел не работал с autoCompleteSceneHeader
**Проблема:** `autoCompleteSceneHeader` заменял текст при каждом изменении, не давая набрать пробел.
**Решение:** Заменить на SmartType (подсказки без автовставки) — пользователь сам выбирает.
**Предотвращение:** Никогда не менять ввод пользователя автоматически.

### Урок 3: Конфликт подсказок smartType и sceneHeaderSuggestions
**Проблема:** Два разных источника подсказок конфликтовали и мешали друг другу.
**Решение:** `sceneHeaderSuggestions` приоритетнее — закрывать smartType, если есть шапка сцены.
**Предотвращение:** Иерархия контекста: конкретный (шапка) > общий (любой блок).

### Урок 4: applySuggestion стирала текст
**Проблема:** Неправильно находила последнее слово для замены.
**Решение:** Использовать `lastIndexOf(' ')` для точного определения границы слова.
**Предотвращение:** Тестировать replace на разных длинах текста.

### Урок 5: Блок участников создавался внизу
**Проблема:** Использование устаревшего индекса при добавлении нового блока.
**Решение:** Использовать `setBlocks(prevBlocks => ...)` для актуального индекса.
**Предотвращение:** Всегда использовать функциональную форму setState при зависимости от предыдущего состояния.

---

## Сессия 01.06.2026 — Архитектурный аудит

### Урок 6: Дублирование интерфейса Scene (АРХИТЕКТУРНАЯ ОШИБКА)
**Проблема:** `SceneNavigator.tsx` имел свой `interface Scene`, дублирующий `Scene` из `scriptStore.ts`. При изменении одного — другой устаревал. Поле `pageCount` vs `pages` вызвало ошибку TypeScript.
**Решение:** Удалить дубль, импортировать `type { Scene } from '../store/scriptStore'`.
**Предотвращение:** Никогда не дублировать интерфейсы/типы! Всегда импортировать из единого источника (store/types).

### Урок 7: Завершение сессии без проверки сохранения (ПРОЦЕССНАЯ ОШИБКА)
**Проблема:** Сказал "сессия завершена", но не убедился что все изменения сохранены и синхронизированы с git. Пользователь заметил что изменения не были закоммичены.
**Решение:** ВСЕГДА проверять `git status` перед завершением сессии. Если есть изменения — коммитить и пушить.
**Предотвращение:** Правило: "Завершение сессии = git status clean + git push successful". Без этого сессия не завершена!

---

## Сессия 01.06.2026 — Смена архитектуры редактора

### Урок 8: Требования пользователя меняют архитектуру (АРХИТЕКТУРНЫЙ УРОК)
**Проблема:** Планировал парсер сцен (text-based), но пользователь предъявил критические требования:
1. Авто-нумерация серий — "1. ИНТ." → "1-1. ИНТ." (автоматически)
2. Drag&Drop сцен с авто-перенумерацией — перестановка сцен с обновлением номеров

Парсер сцен НЕ может качественно реализовать Drag&Drop с авто-перенумерацией (задержки, ошибки репарсинга, потеря текста).

**Решение:** Сразу внедрять Tiptap SceneNode (node-based) — сцены как структурные узлы документа. Это решает оба требования идеально.

**Предотвращение:** Всегда обсуждать критические требования ДО выбора архитектуры. Если требования появляются позже — быть готовым к смене архитектуры.

---

## Сессия 01.06.2026 — Финальные фиксы форматирования и стабилизация

## Сессия 13.06.2026 — Paste Auto-Formatting

### Урок 11: Tiptap `insertContent` vs `replaceSelection`
**Проблема:** `replaceSelection` с block-nodes ломался при вставке внутри paragraph.
**Решение:** Использовать `editor.chain().insertContent(html)` — Tiptap сам корректно вставляет block-ноды.
**Предотвращение:** Всегда предпочитать Tiptap API (`insertContent`) низкоуровневым ProseMirror вызовам.

### Урок 12: `items-center` + `overflow-y-auto` = обрезанный верх
**Проблема:** `items-center` в контейнере с `overflow-y-auto` и `min-h-[800px]` контентом центрировал содержимое — верх уходил за пределы видимости, скролл начинался с середины.
**Решение:** Убрать `items-center`, оставить только `justify-center` + `overflow-y-auto`.
**Предотвращение:** Никогда не комбинировать `items-center` с `overflow-y-auto` и контентом больше viewport.

### Урок 13: SceneNode wrapper ломает существующую структуру
**Проблема:** Добавление `<div data-type="scene">` обёртки вокруг вставленных блоков сломало: 1) Drag&Drop, 2) Навигатор (сцены пропали), 3) Появились лишние полосы в редакторе.
**Причина:** Существующая структура документа НЕ оборачивает сцены в `sceneNode` при создании/редактировании. Обёртка только для вставленного текста создавала смешанную структуру.
**Решение:** Откатить обёртку. Сцены остаются плоским списком блоков.
**Предотвращение:** Не вводить структурные изменения (обёртки) только для одного кейса (paste). Либо менять ВСЮ структуру, либо оставлять плоской.

### Урок 14: Слишком строгие паттерны ломают диалоги
**Проблема:** Сокращенные формы "И" и "И." в паттерне шапки сцены ловили начало слов в диалогах ("И что?", "И ты..."), создавая ложные сцены.
**Решение:** Убрать сокращения — только полные формы "ИНТ", "ЭКСТ", "ИНТ-ЭКСТ". Требовать пробел + заглавную букву после.
**Предотвращение:** Паттерны должны быть максимально специфичны. Если слово может встретиться в обычном тексте — оно не должно быть триггером.

### Урок 15: CSS max-width сжимает текст до вертикали
**Проблема:** `max-width: calc(100% - 3.75cm - 3.75cm)` для диалога сжимал блок до ~8cm, текст разбивался по буквам вертикально.
**Решение:** Убрать `max-width` и `box-sizing`. Диалог должен занимать доступное пространство с отступами.
**Предотвращение:** Никогда не ограничивать width блока с текстом если есть padding'и по бокам.

### Урок 16: Padding-left мешает центрированию
**Проблема:** `padding-left: 6.75cm` с `text-align: center` создавал смещение — текст центрировался в оставшемся узком пространстве справа.
**Решение:** Для центрирования использовать `width: 100%` + `text-align: center`, а не padding с одной стороны.
**Предотвращение:** Для центрирования всегда использовать симметричные отступы или проценты ширины.

### Урок 17: SmartType везде мешает в диалогах
**Проблема:** Подсказки ИНТ/ЭКСТ/время срабатывали во всех блоках, включая диалоги, где они не нужны.
**Решение:** Ограничить SmartType только `sceneHeader`. Другие блоки — `closeSuggestions()`.
**Предотвращение:** Контекстные подсказки должны проверять текущий тип блока перед показом.

### Урок 18: Несинхронизированные паттерны — ложные сцены
**Проблема:** Автоопределение блока использовало один паттерн, а парсер сцен — другой. Диалоги попадали в список сцен.
**Решение:** Синхронизировать паттерны в `autoDetectBlockType` и `parseScenes`. Один источник истины.
**Предотвращение:** Если есть два места с одинаковой логикой — выносить в константу или функцию.

---

## Сессия 30.05.2026 — SmartType для шапки сцены

### Урок 1: Автонумерация мешает вводу
**Проблема:** Автонумерация пересчитывала номера при КАЖДОМ изменении текста, что переписывало то что пишет пользователь.
**Решение:** Убрать автонумерацию из `handleContentChange`. Оставить только при создании новой сцены (Enter/Tab).
**Предотвращение:** Всегда проверять — изменение state пересоздаёт ли блоки? Если да — вешать только на явные действия (создание, удаление).

### Урок 2: Пробел не работал
**Проблема:** `autoCompleteSceneHeader` вызывалась при каждом изменении текста и заменяла «И» на «ИНТ.» даже когда пользователь хотел просто написать слово с пробелом.
**Решение:** Заменить автовставку на SmartType (подсказки без автовставки). Пользователь сам решает — нажать Tab или нет.
**Предотвращение:** Никогда не заменять текст без явного действия пользователя. Подсказка ≠ принудительная замена.

### Урок 3: Конфликт двух систем подсказок
**Проблема:** `smartType` (для персонажей) и `sceneHeaderSuggestions` (для шапки) работали одновременно. При нажатии Tab — `smartType` перехватывал событие и не давал применить подсказку шапки.
**Решение:** Сделать `sceneHeaderSuggestions` приоритетнее. Проверять сначала его, потом `smartType`.
**Предотвращение:** Если есть несколько систем с одинаковыми триггерами (Tab/Enter) — всегда определять приоритет.

### Урок 4: applySuggestion стирала текст
**Проблема:** `applySuggestion` неправильно находила «последнее слово» — использовала `split(/\s+/)` вместо `lastIndexOf(' ')`, что приводило к удалению части текста.
**Решение:** Использовать `lastIndexOf(' ')` для поиска начала последнего слова.
**Предотвращение:** При замене текста — всегда проверять на примерах с пробелами, точками, спецсимволами.

### Урок 5: Блок участников создавался внизу
**Проблема:** Блок `scene_cast` создавался в конце списка блоков вместо сразу после шапки, потому что использовался устаревший массив `blocks` из замыкания.
**Решение:** Использовать функциональную форму `setBlocks(prevBlocks => ...)` для актуального индекса.
**Предотвращение:** Всегда использовать функциональную форму setState если новое значение зависит от предыдущего.

### Урок 8: Нереализованные кнопки — ставить заглушки
**Проблема:** Кнопки "Сохранить", "Настройки", панель "Заметки" — всё были нерабочими элементы без объяснения.
**Решение:** Добавить `alert()` с сообщением что функция в разработке. Заменить панель заметок на экран "В разработке".
**Предотвращение:** Любая кнопка без функции = заглушка с объяснением. Не оставлять нерабочие элементы.

### Урок 7: Предустановленные сцены + null
**Проблема:** DEMO_SCENES показывались при "Написать с нуля". После замены на пустой массив `scenes`, `selectedScene` стал `null` — краш при обращении к `.number`, `.id`.
**Решение:** Заменить `DEMO_SCENES` на `[]`. Добавить `?.` (optional chaining) во все места где используется `selectedScene`.
**Предотвращение:** При замене данных-заглушки на пустой массив — всегда проверять все места где использовалась заглушка. Всегда добавлять fallback для null.

### Урок 6: Не сохранялось в _ai_workspace
**Проблема:** Я забыл сохранять прогресс в документацию. Тратилось время на повторное объяснение, терялся контекст.
**Решение:** Создать чёткую структуру (CURRENT_STATUS, CURRENT_STEP, CHECKLIST, LESSONS_LEARNED) и проверять по чек-листу после каждого шага.
**Предотвращение:** Правило «9 пунктов» из CHECKLIST.md — проверять документацию после каждого шага.

### Урок 9: Кириллица в пути файла — создание "призрачной" папки
**Проблема (31.05.2026):** При создании файла `ScriptEditorTiptap.tsx` через write_to_file, я случайно использовал кириллицу в пути: `kinopлан` вместо `kinoplan`. Это создало вторую папку с похожим именем, в которую попал новый файл. Все последующие команды (npm install, git add, TypeScript проверка) искали файл в правильной папке `kinoplan` и не находили его.

**Последствия:**
- TypeScript ошибки: "Cannot find module"
- Невозможность запустить dev сервер
- Потеря времени на отладку (15+ минут)
- Конфузия в файловой системе

**Решение:** 
1. Удалить "призрачную" папку `kinopлан`
2. Пересоздать файл в правильной папке `kinoplan`
3. Перезапустить dev сервер

**Корневые причины:**
1. **Автокомплит кириллицы:** IDE или система предложила кириллический вариант пути
2. **Невнимательность:** Не проверил путь перед подтверждением
3. **Отсутствие валидации:** write_to_file не проверяет, существует ли путь

**Предотвращение (заповеди):**
1. **Всегда копировать путь:** Использовать `read_file` или `list_dir` для получения точного пути, не писать руками
2. **Проверять после создания:** Сразу после `write_to_file` делать `read_file` для проверки что файл на месте
3. **Только ASCII в путях:** Никогда не использовать кириллицу, пробелы, спецсимволы в путях
4. **Проверять npm install:** Если пакеты установились, но TypeScript не видит модули — проверить что файл в правильной папке
5. **Быстрое обнаружение:** При ошибке "Cannot find module" сразу проверить физическое расположение файла через `find_by_name`

**Индикаторы проблемы:**
- TypeScript: "Cannot find module" после успешного npm install
- Команды cd/ls работают, но файл не находится
- Наличие двух папок с похожими именами (проверять через `list_dir`)

---

## Сессия 01.06.2026 — SceneNode архитектура

### Урок 15: Не записал важное правило сразу (ПРОЦЕССНАЯ ОШИБКА)
**Проблема:** Пользователь объяснил правило "как объяснять" (простым языком, на примерах из приложения), но я не записал его сразу в ОБЩЕНИЕ.md и не создал память. Записал только после напоминания.
**Решение:** Сразу записать правило в ОБЩЕНИЕ.md, создать память в системе, создать связи между документами.
**Предотвращение:** При получении важных правил от пользователя — СРАЗУ записывать в документацию и память. Не ждать напоминания. Правило: "Важное правило = запись в ОБЩЕНИЕ.md + память + связи".

---

## Сессия 01-02.06.2026 — Git и документация

### Урок 16: Выборочный git add оставляет файлы незакоммиченными (ПРОЦЕССНАЯ ОШИБКА)
**Дата:** 02.06.2026  
**Проблема:** Коммитил только отдельные файлы (`git add file1 file2`). Другие изменённые и новые файлы (`SettingsPage`, `SchedulePage`, `alertStore`, `uiStore` и др.) оставались незакоммиченными. Пользователь видел несинхронизированные файлы в IDE.  
**Решение:** Всегда использовать `git add .` — захватывает все изменения включая untracked файлы.  
**Предотвращение:** ПРАВИЛО — команда коммита всегда:
```
git add .
git commit -m "описание"
git push
```
Никогда не перечислять файлы вручную.

### Урок 17: Документация _ai_workspace не обновлялась автоматически (ПРОЦЕССНАЯ ОШИБКА)
**Дата:** 02.06.2026  
**Проблема:** Обновлял `CURRENT_STEP.md`, `CURRENT_STATUS.md`, `ПЛАН_РАБОТ.md` только после напоминания пользователя. Контекст терялся между сессиями.  
**Решение:** Обновлять workspace автоматически — в конце каждой сессии И после каждого значимого изменения кода, без напоминаний.  
**Предотвращение:** ПРАВИЛО — завершение сессии включает:
1. Обновить CURRENT_STEP.md
2. Обновить CURRENT_STATUS.md
3. Обновить ПЛАН_РАБОТ.md
4. `git add . && git commit -m "..." && git push`

---

**Шаблон добавления нового урока:**
```markdown
### Урок N: [Краткое название]
**Дата:** [ДД.ММ.ГГГГ]
**Проблема:** [Что случилось]
**Решение:** [Как исправили]
**Предотвращение:** [Как избежать в будущем]
```

---

**Связанные файлы:**
- [📋 CHECKLIST](../CHECKLIST.md) — чек-листы для проверки
- [📊 CURRENT_STATUS](../CURRENT_STATUS.md) — текущий прогресс
- [🎯 CURRENT_STEP](../CURRENT_STEP.md) — текущий шаг
- [🤝 MY_COLLABORATION_GUIDE](../MY_COLLABORATION_GUIDE.md) — правила работы

## Сессия 13.06.2026 — UX-аудит редактора

### Урок 18: Компонент с `blocks={[]}` — мёртвая фича (UX-БАГ)
**Дата:** 13.06.2026
**Проблема:** `FormatAssistant` в `ScriptPage.tsx` получал `blocks={[]}` и всегда показывал «Форматирование в порядке». Пользователь думал, что сценарий правильно отформатирован, хотя ошибки были.
**Решение:** Создать `extractBlocksFromHtml()` — парсит HTML редактора по `data-type` атрибутам и маппит типы блоков. Подключить к `FormatAssistant`.
**Предотвращение:** При рефакторинге — всегда проверять пропсы дочерних компонентов. `={[]}` или `={undefined}` = мёртвая фича. Правило: если компонент анализирует данные — он должен получать реальные данные.

### Урок 19: Локальный `useState` вместо store = потеря данных (АРХИТЕКТУРНЫЙ БАГ)
**Дата:** 13.06.2026
**Проблема:** `TitlePageEditor` хранил данные в `useState<TitlePageData>()`. При перезагрузке приложения — все поля титульной страницы сбрасывались.
**Решение:** Сделать `TitlePageEditor` controlled component (`data` + `onChange` пропсы). Сохранять в `scriptStore.titlePage` через `updateScript()`. Zustand persist автоматически сохраняет в localStorage.
**Предотвращение:** Правило — данные, которые должны переживать перезагрузку, НИКОГДА не хранятся в локальном `useState`. Только в store. UI-стейт (`showPreview`, `isOpen`) — можно в useState.

---

## Сессия 16-17.06.2026 — SmartType шапка сцены

### Урок 20: Бесконечный цикл setContent — редактор сбрасывался на каждую букву (КРИТИЧЕСКИЙ БАГ)
**Дата:** 17.06.2026
**Проблема:** `useEffect` в `useScriptEditorLogic` вызывал `editor.commands.setContent(initialContent)` при каждом изменении `initialContent`. При наборе текста: `onUpdate` → `onContentChange` → `updateScript` → `currentScript.content` меняется → `initialContent` обновляется → `setContent` сбрасывает редактор. Пользователь не мог набрать пробел, курсор прыгал, подсказки ломались.
**Решение:** Добавить `initialContentLoadedRef` — загружать контент только при создании редактора (монтирование). При смене серии редактор пересоздаётся через `key={currentScript?.id}`.
**Предотвращение:** ПРАВИЛО — `setContent` в `useEffect` всегда должен быть защищён флагом "загружено". Или использовать `onCreate` callback редактора. Никогда не связывать `initialContent` с `onContentChange` напрямую.

### Урок 21: undefined в forEach — fallback на массивы (JS-БАГ)
**Дата:** 17.06.2026
**Проблема:** Убрал `|| []` из `useScriptEditorLogic` при передаче в `useSmartType({ characters, locations, times })`. `characters` и `locations` приходили как `undefined`. `useSmartType` использовал их в `.forEach()` — краш TypeError.
**Решение:** Добавить дефолты прямо в деструктуризации `useSmartType`: `characters: initialCharacters = []`, `locations: initialLocations = []`.
**Предотвращение:** ПРАВИЛО — массивы, которые используются в `.forEach()`, `.map()`, `.filter()` — ВСЕГДА иметь дефолт `= []` при деструктуризации. Даже если тип говорит `string[]`, runtime может быть `undefined`.

### Урок 22: Подсказки времени суток не появлялись в пустом сценарии (UX-БАГ)
**Дата:** 17.06.2026
**Проблема:** `times` в `useSmartType` имел дефолт `['ДЕНЬ', 'НОЧЬ', ...]`, но `useScriptEditorLogic` передавал `smartTypeTimes || []` — пустой массив перекрывал дефолт. SmartType не мог "выучить" времена из пустого сценария.
**Решение:** В `allSuggestions` объединить `baseTimes` (жёстко заданные) с `times` (из сценария): `const mergedTimes = new Set([...baseTimes, ...times])`.
**Предотвращение:** Данные, которые должны быть доступны всегда (независимо от контента) — не полагаться на внешние списки. Дублировать базовые значения внутри компонента/хука.

### Урок 23: Двойная нумерация в serial-шапке (ЛОГИЧЕСКАЯ ОШИБКА)
**Дата:** 16.06.2026
**Проблема:** В `autoDetectBlockType` заменял `upperText` вместо `extType` при формировании `newText`. Получалось `2-1. 1. ИНТ.` вместо `2-1. ИНТ.`
**Решение:** Использовать только `extType` (ИНТ./ЭКСТ.) в `newText`, а не полный `upperText`.
**Предотвращение:** При regex-замене — проверять, что `newText` не включает старый текст целиком. Тестировать на реальных примерах.

### Урок 24: `\b` (word boundary) в JS regex НЕ работает с кириллицей
**Дата:** 20.06.2026
**Проблема:** `isScreenplayContent` использовал `/\b(ИНТ|ЭКСТ)\b/` — для кириллицы `\b` не определяет границу слова, поэтому строка `3-1. ЭКСТ. УЛИЦА...` не распознавалась как сценарий.
**Решение:** Заменить `\b` на явную проверку `[\.\s]` — точка или пробел после `ИНТ`/`ЭКСТ`.
**Предотвращение:** Никогда не использовать `\b` с кириллическим текстом. Всегда использовать явные разделители.

---

**Создан:** 30.05.2026
